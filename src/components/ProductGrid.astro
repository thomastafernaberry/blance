---
import { strapi } from '@strapi/client';
import ProductCard from './ProductCard.astro';

class Product {
	strapiCollectionName = 'products'; 

	strapiURL = 'http://localhost:1337/api';
	strapiToken = '';
	strapiClient = strapi({ 
		baseURL: this.strapiURL, 
		auth: this.strapiToken 
	});

	async searchByName(name: string) : object {
		const collection = await (this.strapiClient).collection(this.strapiCollectionName);
		const products = await collection.find(name);
		const productsArray = [];
		for (const { p, i } of products.data) {
			productsArray.push(p);
		}
		return productsArray;
	}
}

const searchParams = Astro.url.searchParams;
const productsArray = await (new Product).searchByName(searchParams);

interface Props {
	viewMode: string;
}

const { viewMode = 'single' } = Astro.props as Props;
---

<script define:vars={{productsArray}}>
	console.log(productsArray);
</script>

{ viewMode === 'double' ?
<div class="grid grid-cols-2 gap-4 p-3">
</div> :
<div class="grid p-3">
	<ProductCard type="big" />
	<ProductCard type="big" />
	<ProductCard type="big" />
	<ProductCard type="big" />
</div> }

