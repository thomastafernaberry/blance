---
interface Props {
	hex: string;
	slug: string;
}

const { hex = '', slug = '#' } = Astro.props as Props;
const productURL = `/tienda/producto/${slug}`;
---

<div class="color w-[24px] h-[24px] rounded-full outline-1 outline-gray-100 outline-offset-1">
	<a class="block w-full h-full" href={productURL} aria-label="Color disponible"></a>
</div>


<style define:vars={{hex}}>
	div { background-color: var(--hex); }
</style>

