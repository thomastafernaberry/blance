---
import { strapi } from '@strapi/client';
import ProductCard from './ProductCard.astro';

class Product {
	strapiCollectionName = 'products'; 

	strapiURL = 'http://localhost:1337/api';
	strapiToken = '';
	strapiClient = strapi({ 
		baseURL: this.strapiURL, 
		auth: this.strapiToken 
	});

	async searchByName(name: string) : object {
		const collection = await (this.strapiClient).collection(this.strapiCollectionName);
		return await collection.findOne(1);
	}
}

const params = Astro.url.searchParams;
const search = params.get('q');
const productsObj = await (new Product).searchByName(search);

interface Props {
	viewMode: string;
}

const { viewMode = 'single' } = Astro.props as Props;
---

<script define:vars={{ search }}>
	console.log(search);
</script>

{ viewMode === 'double' ?
	<div class="grid grid-cols-2 gap-4 p-3">
		{ productsObj.data.map( p => <ProductCard name={p.name} price={p.price} />) }
	</div> :
	<div class="grid p-3">
		{ productsObj.data.map( p => <ProductCard type="big" name={p.name} price={p.price} />) }
	</div> }

